<% provide(:title, 'Create Group') %>

<div class="row">

  <div class="span-7 offset-1 group-faq">
    <h2>The Basics</h2>
    <ul>
      <li>Groups are used to share expenses with a set of people.</li>
      <li>When you create a group, your group will be assigned a unique ID number. Other people will need this, along with the password you chose in order to join your group.</li>
      <li>You can create and belong to as many groups as you like!</li>
    </ul>
  </div><!-- .group-faq -->

  <div class="span-7">
    <%= form_for(@group, html: {class: "new-group-form"}) do |f| %>
      <h1 class="form-title">Create New Group</h1>

      <%= render "group_error_messages" %>      

      <div class="field control-group">
        <%= f.label :title %>
        <span class="help-text">Describe your group!</span>
        <%= f.text_field :title %>
      </div>

      <div class="field control-group">
        <%= f.label :password %>
        <span class="help-text">People will need this to join your group.</span>
        <%= f.password_field :password %>
      </div>

      <div class="field control-group">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation %>
      </div>

      <%= submit_tag "Create Group", class: "btn btn-blue", id: "submit-validate" %>
      
    <% end %><!-- .new-group-form -->
  </div>
</div>